---
name: web-accessibility
description: Implement web accessibility (a11y) standards following WCAG 2.1 guidelines. Use when building accessible UIs, fixing accessibility issues, or ensuring compliance with disability standards. Handles ARIA attributes, keyboard navigation, screen readers, semantic HTML, and accessibility testing.
tags: [accessibility, a11y, WCAG, ARIA, semantic-HTML, screen-reader]
platforms: [Claude, ChatGPT, Gemini]
---

# Web Accessibility (A11y)

## 목적 (Purpose)

모든 사용자가 접근 가능한 웹 애플리케이션을 구축합니다.

이 스킬은 다음을 도와줍니다:
- WCAG 2.1 AA 수준 준수
- 스크린 리더 지원
- 키보드 네비게이션 구현
- ARIA 속성 적용
- 접근성 테스트 및 검증

## 사용 시점 (When to Use)

- **새 UI 컴포넌트 개발**: 접근 가능한 컴포넌트 설계
- **접근성 감사**: 기존 사이트의 접근성 문제 식별 및 수정
- **폼 구현**: 스크린 리더 친화적인 폼 작성
- **모달/드롭다운**: 포커스 관리 및 키보드 트랩 방지
- **WCAG 준수**: 법적 요구사항 또는 표준 준수

## 입력 형식 (Input Format)

### 필수 정보
- **프레임워크**: React, Vue, Svelte, Vanilla JS 등
- **컴포넌트 유형**: Button, Form, Modal, Dropdown, Navigation 등
- **WCAG 레벨**: A, AA, AAA (기본값: AA)

### 선택 정보
- **스크린 리더**: NVDA, JAWS, VoiceOver (테스트용)
- **자동 테스트 도구**: axe-core, Pa11y, Lighthouse (기본값: axe-core)

## 작업 절차 (Procedure)

상세 코드는 [EXAMPLES.md](./EXAMPLES.md)를 참조하세요.

### 1단계: 시맨틱 HTML 사용

올바른 HTML 요소를 사용하여 의미를 전달합니다.

**작업 내용**:
- `<button>` vs `<div onClick>` - 버튼은 button 요소 사용
- `<nav>`, `<main>`, `<header>`, `<footer>` 사용
- `<h1>` ~ `<h6>` 계층 구조 유지
- `<label>` 과 폼 요소 연결

👉 **상세 코드**: [EXAMPLES.md > 시맨틱 HTML](./EXAMPLES.md#1단계-시맨틱-html-사용)

### 2단계: ARIA 속성 적용

복잡한 UI에 ARIA 속성을 추가합니다.

**작업 내용**:
- `aria-label`, `aria-labelledby` - 레이블 제공
- `aria-expanded`, `aria-hidden` - 상태 표시
- `role` 속성 - 커스텀 컴포넌트에 역할 부여

👉 **상세 코드**: [EXAMPLES.md > ARIA 속성](./EXAMPLES.md#2단계-aria-속성-적용)

### 3단계: 키보드 네비게이션

모든 인터랙티브 요소에 키보드 접근을 보장합니다.

**확인 사항**:
- [x] Tab으로 모든 요소 접근 가능
- [x] Enter/Space로 버튼 활성화
- [x] ESC로 모달/드롭다운 닫기
- [x] 화살표 키로 메뉴 네비게이션

👉 **상세 코드**: [EXAMPLES.md > 키보드 네비게이션](./EXAMPLES.md#3단계-키보드-네비게이션)

### 4단계: 포커스 관리

포커스 순서와 가시성을 관리합니다.

**작업 내용**:
- 포커스 트랩 (모달 내부에 포커스 유지)
- 포커스 복원 (모달 닫을 때 이전 위치로)
- `:focus-visible` 스타일 제공

👉 **상세 코드**: [EXAMPLES.md > 포커스 관리](./EXAMPLES.md#4단계-포커스-관리)

### 5단계: 접근성 테스트

자동 및 수동 테스트를 수행합니다.

**작업 내용**:
- axe-core로 자동 테스트
- 스크린 리더로 수동 테스트
- 키보드만으로 사용 가능한지 확인

👉 **상세 코드**: [EXAMPLES.md > 접근성 테스트](./EXAMPLES.md#5단계-접근성-테스트)

## 출력 포맷 (Output Format)

```
프로젝트/
├── src/
│   ├── components/
│   │   ├── AccessibleModal.tsx
│   │   ├── AccessibleForm.tsx
│   │   └── AccessibleButton.tsx
│   └── tests/
│       └── accessibility.test.ts
└── .eslintrc.js  # eslint-plugin-jsx-a11y 설정
```

## 제약사항 (Constraints)

### 필수 규칙 (MUST)

1. **시맨틱 HTML 우선**: 가능한 한 네이티브 HTML 요소 사용
2. **키보드 접근**: 모든 인터랙티브 요소는 키보드로 접근 가능
3. **대체 텍스트**: 이미지, 아이콘에 alt 또는 aria-label 제공

### 금지 사항 (MUST NOT)

1. **div/span 남용**: 버튼이나 링크를 div로 만들지 않음
2. **outline 제거**: `:focus { outline: none }` 금지 (대체 스타일 필요시만)
3. **자동 재생**: 비디오/오디오 자동 재생 금지

## 메타데이터

### 버전
- **현재 버전**: 1.0.0
- **최종 업데이트**: 2025-01-01

### 관련 스킬
- [ui-component-patterns](../ui-component-patterns/SKILL.md)
- [responsive-design](../responsive-design/SKILL.md)

### 태그
`#accessibility` `#a11y` `#WCAG` `#ARIA` `#screen-reader`
